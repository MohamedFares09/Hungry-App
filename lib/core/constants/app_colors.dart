import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Color (Vibrant Orange)
  static const Color primaryColor = Color(0xFFFF7622);

  // Secondary/Accent Color (Soft Yellow/Gold)
  static const Color secondaryColor = Color(0xFFFFD27C);

  // Background Colors
  static const Color backgroundColor = Color(0xFFF6F8FA);
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF121223);

  // Text Colors
  static const Color textPrimary = Color(
    0xFF121223,
  ); // Dark Blue/Black for Headings
  static const Color textSecondary = Color(0xFFA0A5BA); // Grey for subtitles
  static const Color textLight = Color(0xFFFFFFFF); // White text

  // UI Elements
  static const Color inputFill = Color(0xFFF0F5FA);
  static const Color border = Color(0xFFEDEDED);
  static const Color error = Color(0xFFFF4B4B);
  static const Color success = Color(0xFF2DCE89);
}
